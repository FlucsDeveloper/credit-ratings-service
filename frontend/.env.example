# Credit Ratings Service - Environment Configuration
# Copy this file to .env.local and configure based on your use case

# ===================================
# LLM Provider Configuration
# ===================================
# The system works in 2 layers:
# 1. Regex (always active, free, fast)
# 2. LLM Fallback (optional, used only when regex fails)
#
# Choose ONE provider below or leave all empty for regex-only mode

# ─────────────────────────────────────
# OPTION 1: Regex-only (Recommended to start)
# ─────────────────────────────────────
# ✅ 100% free, no API calls
# ✅ Works for 70% of cases (companies in database + well-formatted pages)
# ✅ Fast and deterministic
# ❌ May fail on poorly formatted pages or news articles
#
# To use: Leave all API keys commented out, or set:
LLM_PROVIDER=none

# ─────────────────────────────────────
# OPTION 2: OpenAI GPT-4o-mini (Production)
# ─────────────────────────────────────
# ✅ Best quality (95%+ accuracy)
# ✅ Reliable API (99.9% uptime)
# ✅ Enterprise support available
# 💰 Cost: ~$14/year for 1000 queries/day
#
# Get key at: https://platform.openai.com/api-keys
# Format: sk-proj-...
#
# OPENAI_API_KEY=sk-proj-...
# LLM_PROVIDER=openai

# ─────────────────────────────────────
# OPTION 3: DeepSeek-V2 (Development)
# ─────────────────────────────────────
# ✅ Good quality (85% accuracy)
# ✅ Cheaper (~$3/year for 1000 queries/day)
# ⚠️ Requires credits (add $5-10 at platform.deepseek.com)
# ⚠️ "Insufficient Balance" error = no credits
#
# Get key at: https://platform.deepseek.com
# Format: sk-...
#
# DEEPSEEK_API_KEY=sk-...

# ─────────────────────────────────────
# OPTION 4: Ollama (Local/Private)
# ─────────────────────────────────────
# ✅ Free, runs locally
# ✅ 100% private (no data leaves your network)
# ⚠️ Requires local setup (Ollama installed)
# ⚠️ Slower on CPU (fast with GPU)
#
# Setup:
# 1. Install: brew install ollama
# 2. Download model: ollama pull llama3.1:8b
# 3. Start server: ollama serve
# 4. Set URL below:
#
# OLLAMA_BASE_URL=http://localhost:11434

# ===================================
# Application Configuration
# ===================================

# Next.js
NODE_ENV=development

# ===================================
# Notes
# ===================================
#
# REGEX-ONLY MODE (No LLM):
# - Leave all LLM keys empty
# - Or set: LLM_PROVIDER=none
# - Works well for well-formatted rating agency pages
# - 100% free, fast, deterministic
#
# LLM FALLBACK MODE:
# - Set any ONE of the API keys above
# - LLM is used only when regex fails
# - Improves extraction from poorly formatted pages
# - Better for scraping news articles, press releases
#
# COST ESTIMATES (per 1000 extractions with LLM fallback):
# - OpenAI GPT-4o-mini: ~$0.50-1.00 (assuming 30% LLM usage)
# - DeepSeek-V2: ~$0.10-0.20 (cheaper but requires credits)
# - Ollama: $0.00 (free, but needs local GPU/CPU)
# - Regex-only: $0.00 (always free)
